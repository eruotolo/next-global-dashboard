'use client';

import PagePermissionsTable from './PagePermissionsTable';

// Wrapper para mantener compatibilidad con imports existentes
export default function PagePermissionsManager() {
    return <PagePermissionsTable />;
}
